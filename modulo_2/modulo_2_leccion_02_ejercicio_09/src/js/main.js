'use strict';
const textPartner = document.querySelector('.title');

const namePartner = 'Rocío Dols';
const numberLetter = namePartner.length;

textPartner.innerHTML = `El nombre de mi compañera es ${namePartner} y está compuesto por ${numberLetter} caracteres.`;
